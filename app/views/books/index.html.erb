<table>
  <thead>
    <th>Title</th>
    <th>Thoughts</th>
  </thead>
  <% for book in @books do %>
    <tbody>
      <td><%= book.title %></td>
      <td><%= book.thoughts %></td>
    </tbody>
  <% end %>
</table>

<%= will_paginate @books %>